# next-js-portfolio

# Install Node Js
    -   https://nodejs.org/en
# Create Next js Project:
    -   npx create-next-app@latest
# Run the Development Server
    -   Run npm run dev to start the development server.
    -   Visit http://localhost:3000 to view your application.
    -   Edit app/page.tsx (or pages/index.tsx) file and save it to see the result in your browser.
---------------------------------------------------
# command + shift + p -> type 'simple browser' to open browser within vs code

# Tailwind CSS IntelliSense
    - editor.quickSuggestions -> Turn On
    
------
# rafc to create below code shortcut
    import React from 'react'

    export const HeroSection = () => {
        return (
            <div>HeroSection</div>
            )
    }
------